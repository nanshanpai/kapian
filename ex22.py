# _*_ coding: utf-8 -*-
# print() 输出 数字运算 （+-*/，%余数，+=  ，<,>,<=,>= 
#\n 换行  \\ 斜杠 转义符号
# input() 文本输入 from sys import argv script, first, second,....= argv  在运行文件时导入参数，解包
# def aaa(a,b,c....):   定义函数  "...%d 数值.....%s 字符....%r 调试用，输出原型....." %(a, b, c)
# opend(f, 'w') 打开文件f.read() 文件的读取 f.write() 文件的写入 ，f.close（）文件的关闭 f.seek()返回第一字节   
# f.realine()    按行读取 一行行读取 f.truncate() 清空文件
#return a+b 返回结果  len(f) 文本字符串长度